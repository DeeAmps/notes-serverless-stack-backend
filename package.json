{
  "name": "notes",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "AWS_PROFILE=iamadmin-general sst test",
    "start": "AWS_PROFILE=iamadmin-general sst start",
    "build": "AWS_PROFILE=iamadmin-general sst build",
    "deploy": "AWS_PROFILE=iamadmin-general sst deploy",
    "remove": "AWS_PROFILE=iamadmin-general sst remove"
  },
  "eslintConfig": {
    "extends": ["serverless-stack"]
  },
  "dependencies": {
    "@serverless-stack/cli": "0.69.5",
    "@serverless-stack/resources": "0.69.5",
    "aws-cdk-lib": "2.15.0"
  }
}
